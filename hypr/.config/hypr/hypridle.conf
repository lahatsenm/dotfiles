general {
	#lock_cmd = if [[ "$(dms ipc call lock isLocked)" == "false" ]]; then dms ipc call lock lock; fi
    lock_cmd = pidof hyprlock || hyprlock
}

listener {
	timeout = 10
	on-timeout = loginctl lock-session
}
